#include <iostream>
#include "Monitor.hpp"

int add(int a, int b)
{
    return a + b;
}

int main()
{

    // Monitor monitor(0, &add);
    // std::cout << monitor;

    // monitor = 20;
    // std::cout << monitor;

    // monitor = 30;
    // std::cout << monitor;

    // Monitor monitor2(10, &mul);
    // std::cout << monitor2;
    // monitor2 = 20;

    return 0;
}